services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 5000
      APP_URL: http://localhost:5000
    ports:
      - "5000:5000"
